NODE_ENV=development
PORT=3000
DATABASE_URL="postgresql://batam_erp:batam_erp@localhost:5432/batam_erp?schema=public"
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"
NEXT_PUBLIC_DEV_TENANT_ID=""
NEXT_PUBLIC_DEV_USER_ID=""
# In development, use the seeded user's IDs in these headers:
# x-dev-user-id and x-tenant-id.
# Production must configure Cognito JWT verification; development headers are rejected there.
