import{describe,expect,it,vi}from'vitest';import{AdminWorkspaceService}from'./admin-workspace.module.js';
const identity:any={tenantId:'10000000-0000-4000-8000-000000000001',userId:'20000000-0000-4000-8000-000000000001',requestId:'test',permissions:new Set(['content.manage','content.approve'])};
describe('promotion security',()=>{it('prevents submitter self approval',async()=>{const p:any={promotion:{findFirst:vi.fn().mockResolvedValue({id:'p',tenantId:identity.tenantId,status:'DRAFT',approvalStatus:'PENDING_REVIEW',submittedById:identity.userId,packages:[{packageId:'30000000-0000-4000-8000-000000000001'}]})}};await expect(new AdminWorkspaceService(p).reviewPromotion(identity,'p',{action:'APPROVE'})).rejects.toThrow(/tidak boleh/)});it('rejects package ids that are not all owned by the current tenant',async()=>{const p:any={travelPackage:{findMany:vi.fn().mockResolvedValue([])}};await expect(new AdminWorkspaceService(p).createPromotion(identity,{code:'PROMO-1',title:'Promo',discountType:'PERCENTAGE',discountValue:10,startsAt:'2026-09-01T00:00:00Z',endsAt:'2026-10-01T00:00:00Z',packageIds:['30000000-0000-4000-8000-000000000001']})).rejects.toThrow(/tenant/)})});
