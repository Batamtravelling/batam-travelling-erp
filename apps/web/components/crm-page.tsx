import Link from 'next/link';
import { CrmList } from './crm-list';
const nav=[['Dashboard','/dashboard'],['Leads','/crm/leads'],['Customers','/crm/customers'],['Packages','/sales/packages'],['Quotations','/sales/quotations'],['Bookings','/bookings'],['Operations','/operations/trips'],['Finance','/finance/invoices'],['Reports','/reports'],['Settings','/settings']];
export function CrmPage({kind}:{kind:'leads'|'customers'}){
  const title=kind==='leads'?'Leads':'Customers'; const description=kind==='leads'?'Kelola inquiry, follow-up, dan progres pipeline sales.':'Satu riwayat customer untuk CRM, quotation, booking, dan dokumen.';
  return <main className="shell"><aside><Link className="brand" href="/dashboard">BT<span>ERP</span></Link><nav>{nav.map(([name,href])=><Link key={href} className={href===`/crm/${kind}`?'active':''} href={href}>{name}</Link>)}</nav><small>Batam Travelling<br/>Tenant Owner</small></aside><section className="content"><header><input aria-label="Cari data" placeholder="Cari customer, booking, quotation..."/><button>Notifications</button><button className="avatar">TO</button></header><div className="crumb">ERP / CRM / {title}</div><div className="heading"><div><h1>{title}</h1><p>{description}</p></div><button className="primary">+ Tambah {kind==='leads'?'lead':'customer'}</button></div><div className="kpis"><article><span>Total</span><strong>API</strong><em>Tenant-scoped records</em></article><article><span>Status</span><strong>Live</strong><em>Terhubung ke backend</em></article><article><span>Keamanan</span><strong>RBAC</strong><em>Permission diperiksa API</em></article></div><section className="panel"><div className="panelHead"><h2>{title} terbaru</h2><button>Filter</button></div><CrmList kind={kind}/></section></section></main>;
}
